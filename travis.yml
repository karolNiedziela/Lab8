sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t karolniedziela/Lab8 -f Dockerfile.dev .

script:
  - docker run -e CI=true karolniedziela/Lab8 ng test --karma-config karma.conf.js fibonacci
